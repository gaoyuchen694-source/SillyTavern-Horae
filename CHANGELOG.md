# Horae 更新日志 / Changelog

> 最近版本的更新内容请查看 [README.zh-CN.md](README.zh-CN.md)

---

## v1.17.1

### 叙事日历

- 日历只展示未被总结覆盖的原始关键事件；事件时间线继续保留并显示总结卡片，发送给 AI 的总结与记忆注入逻辑不变。
- 每日默认展示最多 6 条关键/重要事件，其余事件可按需展开；同日高度相似的重复事件会在日历中合并。
- 日期详情新增地点与关系变化，并继续显示计划、物品和时间异常；所有筛选与展开状态仅在运行时计算，不改写聊天来源数据。
- 补齐六种界面语言、移动端触控尺寸、键盘焦点与回归测试。

---

## v1.17.0

### 叙事日历与状态界面

- 新增叙事日历，可按公历、自定义历法或奇幻日期浏览事件、计划期限、物品变化与时间异常；浏览其他日期不会修改当前故事时钟。
- 状态页新增时钟可信度、当前与逾期计划数、当前与归档物品数，以及时间倒退异常提示。
- 时间线页新增日历/事件分段视图、日期周期导航、返回当前剧情日和日期详情跳转。
- 计划和物品列表新增生命周期、数量与递减状态标记，并补齐移动端触控尺寸、键盘焦点与减少动画适配。

### 稳定性与数据安全

- 计划与物品改用稳定 ID 和覆盖层保存用户编辑；历史重放或重新扫描不会覆盖手动修改，已删除物品不会复活。
- 装备穿脱保留原物品稳定 ID，兼容无 ID 的旧装备快照。
- 加强动态文本、图标与楼层跳转参数的转义和校验。
- 六种界面语言补齐日历、记忆健康度与生命周期文案。

### 兼容性

- 继续直接读取原 Horae 数据，无需导出、迁移或重新建立对话。

---

## v1.16.0

### 记忆与时间

- 新增同日权威记忆账本：当天所有有效事件直接进入上下文，不受普通时间线开关、上下文深度或向量阈值影响，下午仍可稳定记住上午剧情。
- 新增可重放的主线故事时钟；闪回不会推进主线，无依据的时间倒退会被拦截并记录异常，跨日但时刻不明时不会继承前一天的具体时刻。
- 新增物品与计划生命周期协议和历史重放：支持数量变化、转移、完成、取消、到期及每日递减；`dueAt` 只表示计划/截止时间，跨到第二天不会清除安排在几天后的计划，逾期后只标记为 `overdue` 并继续保留。
- 编辑、删除或切换 swipe 后，增强状态会从有效楼层重新构建；计划面板操作使用覆盖层，不再破坏历史来源数据。

### 兼容与召回

- 原 Horae 的 `item`、`item-`、`agenda`、`agenda-` 数据可直接读取，无需迁移或重新导入。
- 修复长期向量记忆被未隐藏 AI 楼层整体排除的问题；向量文档增加计划、物品变化、NPC 身份与关系事实。
- 批量扫描、普通提示和向量摘要提示统一采用新的时间与生命周期协议，同时保留旧格式解析兼容。

---

## v1.15.0

### 向量记忆快照

- **「创建携带记忆新对话」携带向量记忆**：confirm 弹窗内勾选后，源对话的向量索引会以 `.json` / `.json.gz` 快照形式打包并自动挂到新对话名下用于召回，同时下载一份本地备份。轻负担，手机端 / 云酒馆均可使用。
- **链式传递**：源对话已挂载的祖先快照也会随之转移到新对话名下，多代「创建携带记忆新对话」不再丢失早期记忆。toast 会显示转移的祖先快照数量。
- **快照管理 UI**：向量设置区新增快照列表，可手动导入 `.json` / `.json.gz` 快照、查看维度与导出时间、移除单条快照。维度与当前模型不一致的快照在挂载阶段自动跳过。
- IndexedDB 增加 `memorySnapshots` store。

---

## v1.14.0

### 辅助 API

- 新增独立「辅助 API」设置区块，可填写 OpenAI 兼容端点、API Key 与模型，并按用途选择是否启用。
- 辅助 API 使用范围拆分为三组：
  - AI 分析、楼层/底部栏魔术棒、发送前自动补全上一条 AI 消息时间线。
  - 自动总结、二次总结、AI 智能补全。
  - 手动多选压缩，默认关闭；若辅助 API 破限不足，可关闭此项改走酒馆原生通道并携带主预设。
- 辅助 API 请求改为串行队列，避免自动总结、魔术棒、智能补全等后台任务并发触发副端点限流。
- 新增「辅助 API 失败时回退主 API」选项，默认关闭，避免副端点失败后继续消耗主 API 速率。
- 旧版自动总结副 API 配置会自动迁移到新的辅助 API 栏位，并清空旧字段，避免重复保存密钥。
- 辅助 API 的地址、密钥、模型不会写入 Horae 配置档导出白名单，防止分享配置时泄露。

---

## v1.13.0

### Horae Port

- 新增 Horae Port 系统，开放 `window.Horae.registerPort()` / `unregisterPort()` / `refreshPorts()`，支持外部组件挂载到底部栏、状态页、抽屉分页、消息面板与 RPG HUD。
- 新增 Data Provider 机制，开放 `registerDataProvider()` / `unregisterDataProvider()`，第三方脚本可按需桥接自己的状态数据；Horae 不内置任何外部变量系统依赖。
- 端口上下文提供 Horae 聚合状态、RPG 快照、消息 meta、Provider 快照与常用 helper，支持 `render` / `update` / `dispose` 生命周期。
- 新增 `portApiVersion`，为后续端口协议兼容提供稳定判断入口。

### 文档

- 新增 `Horae端口系统说明.md`，面向扩展作者说明端口 API、插槽、生命周期、Provider、主题约定与安全边界。
- 新增 `Horae状态栏接入说明.md`，面向角色卡/正则状态栏作者说明如何通过 `window.parent.Horae` 读取 Horae 状态。

---

## v1.12.0

- 新增公开只读 API（`window.Horae`）：`isEnabled()`、`getLatestState()`、`getEvents()`、`getSettings()`、`version`，供其他扩展和预设脚本读取 Horae 状态；设置变更通过 `eventSource` 广播 `horae:settingsChanged` 事件。
- 基于 baibai-git 的 PR #5 整合。
- 针对 issue #4 的需求新增 Gemini Embedding API 兼容。
- 向量召回查询改为单次 Embedding 的合并格式：
  - `[当前情境] <time/location/characters/events from latest AI state>`
  - `[玩家输入] <latest user input>`
- Rerank 查询同步使用合并后的召回查询，不再只使用玩家输入文本。
- 状态查询会附带时间信息，优先使用最新 AI 回合时间，缺失时回退到聚合状态时间。
- 移除向量召回运行时的 fallback 路径，不再用上一条 AI 回复重试召回。
- 新增向量召回参数预设，可在小模型、大模型、Rerank 建议和用户自定义参数之间快速切换；默认回到小模型建议（Top-K 5 / 阈值 0.72）。
- 新增有效召回阈值的运行日志，便于调试召回效果。
- 修复楼层 AI 分析时会触发向量召回的问题。
- 剧情轨迹注入支持“原版合并注入”和“独立前置注入”双模式，默认保留原版合并注入。

---

## v1.10.0

全面重构 RPG 系统，新增 6 个 RPG 子模块、据点经营系统、装备多种族模板等功能。

### RPG 模式

- **子模块独立开关**：装备栏、声望、等级、货币、据点各为独立 checkbox，按需组合。关闭时零 Token 消耗
- **角色卡标签分页**：角色详情区改为分页标签（属性/技能/装备/声望/货币），仅显示已开启的模块
- **只显示在场角色**：顶部角色卡和底部 HUD 根据 `characters_present` 过滤，离场角色数据保留但不显示

### 新增功能

- **等级/经验值系统**：AI 通过 `level:` / `xp:` 标签输出等级变化。经验值公式：升级所需 = 等级 × 100，等级差 ≥10 的低级挑战仅得 1 点经验
- **货币系统**：自定义币种和兑换比率，每个币种可设置 emoji 图标，AI 通过 `currency:归属|币名=±增量` 修改
- **声望系统**：自定义势力声望分类和数值范围，支持细项（子维度）
- **装备栏系统**：按角色独立格位，内置人类/兽人/有翼族/马人/拉弥亚/恶魔六种预设模板，支持自存模板
- **据点/基地系统**：多层级树形结构，AI 通过 `base:` 标签自动更新，支持手动管理和导出/导入

---

## v1.9.0

### 自动摘要 & 副 API

- **副 API 多轮结构改造**：重构摘要请求消息结构，采用 system/assistant/user 多轮交替格式，大幅提升 NSFW 内容摘要通过率
- **模型下拉选单**：副 API、Embedding、Rerank 三处模型设置均从文本输入改为下拉选单
- **自动摘要警告优化**：不再因部分楼层缺少时间戳就完全阻止摘要

### 向量记忆

- **全文剔除标签**：向量设置中新增「全文剔除标签」输入框，避免小剧场/旁白等非剧情内容被 AI 召回
- **番外消息隔离**：标记为「番外」的消息不再被向量索引和召回

---

## v1.8.0

- **RPG 模式**：开启后 AI 通过 `<horaerpg>` 标签输出角色属性和技能变化，与现有 `<horae>` 标签互不干扰
  - **自定义属性条**：默认 HP/MP/SP 三条，用户可自由增删、修改显示名称和颜色（如改 MP 为「灵力」、新增「精力值」「慾望值」等）
  - **状态异常追踪**：支持昏眩、流血、冰冻、灼烧、石化等数十种状态关键词，自动匹配差异化图标显示
  - **技能系统**：记录技能归属、等级和效果描述，技能与 NPC 角色通过编号联动，支持手动添加和 AI 自动生成
  - **底部 HUD 状态栏**：消息面板上方渲染在场角色的简易状态条，显示属性条和异常状态图标，自动跟随底部栏的宽度和偏移设置
  - **RPG 分页**：顶部面板新增 RPG 标签页，展示属性条配置、各角色详细属性和状态列表、技能列表
  - **条件注入**：关闭 RPG 模式时不注入任何提示词，零 Token 消耗；发送属性条和技能列表可独立开关
- **RPG 面板美化**：自助美化工具新增「RPG 状态栏」区域，支持设置背景色和透明度。RPG HUD 纳入主题系统，跟随日夜模式和自定义主题变量

---

## v1.7.0

- **向量记忆 (Beta)**：基于本地 AI 模型的智能回忆系统，专为搭配「自动摘要 & 隐藏」设计。当对话中涉及历史事件时，插件会自动从被隐藏的旧消息中精准召回相关时间线片段，注入上下文供 AI 参考
  - **全程本地运算**：使用浏览器内置 Web Worker 运行轻量 AI 模型，不消耗额外 API 额度
  - **双模型可选**：`bge-small-zh-v1.5`（中文优化）和 `multilingual-e5-small`（多语言支持）
  - **智能检索**：结合意图识别、结构化查询和语义搜索三层检索策略
  - **全文回顾**：高置信度的召回结果可发送原始正文，让 AI 获得完整的叙事细节

---

## v1.6.0

- **自助美化工具**：可视化美化面板。色相条、饱和度/亮度/日夜模式滑杆、图片装饰。所有修改实时预览，可导出 JSON 分享
- **新手导航**：首次使用自动触发交互式教学
- **副 API 破限注入**：自动摘要的独立 API 请求现在会自动注入 SillyTavern 的 jailbreak 提示词和 `max_tokens` 参数

---

## v1.5.0

### 新增功能

- **情绪/心理状态追踪**：追踪在场角色的情绪变化。Token 消耗极低——AI 仅在情绪发生明显变化时才输出 `mood:` 标签
- **关系网络**：记录角色之间的关系（朋友、恋人、上下级、宿敌等）。展示完全由插件从数据库读取渲染，不消耗 AI 输出 Token
- **场景记忆**：记录地点的固定物理特征，保持跨回合场景描写一致。智能检索——仅发送匹配到的那一条描述
- **自动摘要 & 隐藏**：超过阈值自动触发全文摘要，原始楼层 `/hide` 省 Token
- **独立 API（真并行）**：自动摘要可配置独立的 OpenAI 兼容端点，实现与 AI 回复的真并行

### 改进

- **全局表格数据按卡分离**
- **智能记忆压缩**：多选模式下可将多条事件交给 AI 压缩合并为一条摘要

---

## v1.4.0

- **好感度小数点支持**
- **顶部图标开关**
- **时间线插入功能**：长按时间线事件弹出操作菜单，支持在上方/下方添加事件或插入摘要页
- **表格填写提示词自定义**
- **表格 AI 理解增强**：发送给 AI 的表格数据带有坐标标注
- **表格数据清理修复**

---

## v1.3.0

- **全局表格**：自定义表格新增「全局」和「本地」两种作用域
- **行列锁定**：右键/长按表格单元格可锁定行、列或单个格子
- **主题系统**：内置「樱花粉」「森林绿」「海洋蓝」三套预设主题
- **自助美化**：新增 `THEME.md` 文档
- **日夜模式**
- **自定义 CSS**
- **消息面板宽度**
- **正则置底**
- **时间线多选**

---

## v1.2.0

- **自定义提示词**：设置中新增提示词编辑器
- **AI 摘要功能新增**：可中断生成，新增 NPC 和好感度勾选项
- **摘要审阅弹窗**
- **好感度显示开关**
- **好感度删除**

---

## v1.1.0

- **AI 智能摘要**：批量分析历史消息，自动提取剧情事件和物品
- **Token 计数器**
- **swipe 适配**
- **美化兼容**
- **正则修改**
